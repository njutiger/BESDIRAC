Website
{
  Schema
  {
    BES 
    {
      Introduction = info/External/display?site=www.python.org, Introduction, all
      Help = info/External/display?site=docs.python.org/2/, Document, all
      Transfer
      {
        Info = transfer/info, Transfer Info, all
        Transfer Requests = transfer/monitor, Transfer Requests, all
        Accounting = transfer/acct, Accounting, all
      }
      Dataset = transfer/dataset, Dataset, all
    }
    Help 
    {
    }
  }
}
